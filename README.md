Respository containing all examples and notes for the Github Actions course.

Github action
Workflows define at repository level, define which triggers actually start the workflow compose for one or more jobs.
Jobs are define at the workflow level, define execution environment they are run, are compose by one or more steps, run in parallel by default.
Steps define at the jod level, define the actual script or gh action that will be executed, run in sequancilly by default.
